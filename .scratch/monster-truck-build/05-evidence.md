# Ticket 05 evidence

Ticket/review base: `5b606f7b80880a4e6a9a2f3d6936bd7f8d56a039`. Implementation remains uncommitted for the orchestrator's single ticket commit. Spec base: `d261b031d2750f16c6b8c544b07f7e051fd87a2d`.

The host-owned attempt now latches serious observations independently of the bounded recent-event history, commits one immutable copied failure assessment, permits six seconds of physical aftermath, then opens shared results. Timeout, crushing, ravine fall, overturn and unanimous concession all use that flow. The six-second duration remains a checkpoint candidate. The examiner's temporary offline line is “We will leave it there.” Scoring cannot resume or revoke failure during physical recovery. Physical observations remain accepted during aftermath to allow collisions to eject operators without changing the assessment.

Escape offers each current participant Concede test or Withdraw agreement, with the current count. Time runs throughout. Changeable Retry/Waiting room decisions require three matching choices. Commands validate attempt, phase, participant and monotonic per-participant sequence. Departure clears choices and abandons unfinished attempts; an assessment already committed remains immutable until the room transition completes. Retry creates a new identity, resets controls/recovery/results/choices, fades and acknowledges readiness on every peer before fresh synchronized arrival beside the secured truck. Booking and chairs are bypassed. Existing guest-loss return and host-loss own-room behavior remain.

The rough apron includes a raised side bank that physically rolls the truck onto its side; the unsupported apron edge supplies a fall; tyre collision geometry supplies catastrophic compression. This is temporary checkpoint geometry, not the authored route. The side-bank threshold and roll speed require human validation. The test fixture isolates each with real truck/terrain/learner geometry. The results panel leaves the world visible, shows the shared failed assessment and minor count (zero until scoring ticket 12), and shows group choice counts. Personal best and passing records await 12/17; marked-sheet presentation awaits 26.

## Validation

Uses the exact-source isolated Mac verification copy and matching native GodotSteam described in `04-evidence.md`; shipping Windows binaries/manifest are unchanged. Native Steam login is unavailable. ENet scene fixtures are protocol/collision checks, never human or Steam acceptance.

- `05-red.log` is a genuine behavioral failure for serious accident not reaching aftermath; `05-green.log` passes the implemented behavior. `05-choices-red.log` fails the next missing choice capability; `05-choices-green.log` passes. The retained public-boundary suite now exercises actual commands rather than only API existence.
- `05-verify_failure.log` covers immutable outcomes and copied result isolation, serious/zero ties, duplicate scored observations, aftermath timing, changed/withdrawn choices, unanimous retry with clean state, old-attempt rejection, snapshot recovery, and guest departure versus final concession.
- Full suite ran RoomState, attempt, driving, recovery, avatar conversion, learner motion, reception input, failure, and integrated recovery scene (`05-verify_*.log`). Those runs exited 0 at that point. The subsequent aftermath fix exposed an outdated boundary assertion in a focused rerun; that assertion was corrected and the final focused boundary rerun passed, as recorded below. Native sandbox log/editor-settings and certificate messages are environmental; Steam unavailable is explicit.
- `05-scene-final.log`: production collision fixture passes actual bank overturn, unsupported ravine drop, tyre crushing, impossible-rescue concession and aftermath. Spec review found post-settlement operator ejection was initially blocked; the regression now proves a real hull impact ejects an occupied learner after timeout while preserving the assessment. No failures.
- Initial `05-ready.log` reached PASS but exposed a real empty temporary WAV playback failure. Sandboxed `say` had returned success with zero audio bytes. The generator now validates nonzero WAV frames; orchestrator rerendered with native permission, producing 1.047 seconds of mono 22,050 Hz 16-bit speech. Final verification supersedes that initial audio failure.
- `05-import-final.log`: no GDScript/parse errors. New UID/import metadata retained.

## Two-axis review

Standards found no documented violations; its optional repeated-classification suggestion was applied as `SERIOUS_ACCIDENTS`. Spec identified aftermath ejection suppression; fixed with the integrated regression above. Final follow-up and extended network/display results are recorded below when complete.

## Later-ticket ownership

- 06: Three humans over Steam evaluate handling, rescue, aftermath pacing, shared failure and desire to retry. No agent fixture proves fun, fairness or shipping responsiveness.
- 09–12: Replace rough bank/apron and connect actual route/hazard observations; implement full progress, minor incident scoring and passing in 12.
- 17: Personal records and retained-best durability/presentation data.
- 23: Replace the single temporary offline failure line/voice with complete examiner category coverage and alternatives.
- 25: Replace rough roll/compressed learner aftermath with authored wrecks, debris and ragdolls while retaining failure immutability.
- 26: Replace the rough shared results panel with marked-sheet assessment and finished retry/return presentation.

## Final verification

- `05-boundary-final.log`: exit 0, PASS. After the physical-aftermath fix, the recovery assertion correctly checks that the assessment stays failed, rather than incorrectly requiring all subsequent physical observations to be rejected. This focused rerun supersedes the earlier boundary validation after the subsequent aftermath behavior change.
- `05-ready-final.log`: orchestrator's native windowed complete ready-up/transition/guest-loading-loss/host-loss fixture exited 0, final PASS, with no script or audio playback errors after the valid WAV import.
- `05-network.log`: original three-peer boarding/driving/recovery/guest-loss regression exited 0 with final PASS, retaining the previously documented teardown-only ObjectDB/resource warning.
- `05-failure-network-final.log`: focused native windowed three-peer `--failure --capture-results` fixture exited 0, emitted both `FAILURE` verification and final `PASS`. Actual RPC concession reached all peers, the offline examiner audio played, all peers waited through aftermath, mixed decisions stayed in results, a changed third decision began a fresh synchronized attempt, and truck/choices/assessment reset. This run has no script/audio errors or teardown warnings. The intermediate combined frame-capped attempt did not finish and is not evidence of success.
- `05-results.png`: captured from the integrated peer viewport and inspected. The rough failed assessment, reason, minor count and both group-choice counts are readable over the visible world. The fixture camera is beneath rough truck geometry; this is UI legibility evidence only, not finished accident composition or human enjoyment.
- Both review axes completed. Standards' optional classification duplication was corrected. Spec's aftermath ejection issue was corrected and the real collision regression passed. No gameplay source was changed after the final network/display verification; only the boundary test's assertion was corrected and rerun as described above.
