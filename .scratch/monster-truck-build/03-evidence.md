# Ticket 03 evidence

Spec base: `d261b031d2750f16c6b8c544b07f7e051fd87a2d`.
Ticket/review base: `ba2e6c9f72001665e39d97aba480d5dd2f0cb007`.
Reviewed `git diff <ticket-base>` and untracked deliverables; HEAD remains fixed for the orchestrator's single commit. Status remains claimed.

## Implemented and verified

The attempt public boundary accepts gradual held front/rear steering, bounded at 0.6 radians with 1.8 radians/second adjustment. A/D stays truck-relative. W accelerates in the persistent selected direction, S brakes, R switches only below the stopped threshold, and Space separately toggles persistent parking brake. Occupancy release immediately clears held commands; taking a control requires releasing old physically held keys first.

Held packets resend at 60 Hz and expire after 0.35 seconds. Attempt identity, operator generation, and monotonic held sequence reject obsolete commands. Reliable action sequences are independent of held sequences, so a later held packet cannot swallow an earlier parking press. Full 20 Hz snapshots carry persistent controls and other operators' current commands/ages. DEFLATE compression keeps the added snapshot fields below the observed ENet MTU warning; frequent traffic uses plain unreliable channels. Client replay advances truck and supported learner together, with visual-only small truck corrections and no historical replay audio.

`03-red.log` captured the missing driving-command behavior before implementation. `03-green.log` then passed the first vertical slice. The expanded `03-verify_driving.log` has zero failures for limited steering, vacancy/handover, stale/reordered held state, stopped-only direction, reverse acceleration, toggle deduplication, persistent parking, occupancy generation, snapshot recovery, and fresh-attempt reset.

The full existing suite was run once after implementation: RoomState, attempt, driving, Steam avatar conversion, learner motion, reception input, display ready-up, and the three-peer launch/boarding harness all printed their success results. The reception harness also printed its existing `Failed to get image size.` diagnostic. Final focused display rerun after adding gate collision coverage exited **0**, with no script errors or failed assertions (`03-display.log`). Earlier exploratory red logs retain the expected missing-method check and sandbox certificate warnings; they are not final success evidence.

The retained development ENet harness uses three isolated client worlds. It physically walks from ground into the cab and onto the roof, verifies moving release/free look, occupancy contention and short interruption recovery, then operates all three controls through host/network commands. It demonstrates a moving two-axle turn, rejection of R while moving, a braked stop, reversing, duplicate parking presses, and persistent state agreement. A real gate collision stops the truck and completes impact-audio playback. Ground setup/initial motion used to isolate footing remain labeled fixtures; the shipping controlled-motion helper has been removed. This is protocol/scene evidence, not human cooperation or Steam-feel acceptance.

Visual QA inspected `03-rough-truck.png` and `03-front-sight.png`, `03-pedals-sight.png`, `03-rear-sight.png`. The first closeups caught overlapping text; the final captures show readable timer/direction/parking indication at pedals and physical needles plus axle text beside steering. Bonnet, pillars and rear panel obscure nearby ground. The rear opening was narrowed correctly after an initial full-width panel blocked boarding. Contextual hints expire, H restores them, and visible controls highlight. No global timer HUD was added.

`assets/monster_truck/temporary_request.wav` is 195138 bytes of offline System.Speech output, generated by the retained adjacent PowerShell source. It plays on synchronized arrival with matching subtitles. Basic runtime procedural engine pitch follows speed, and gate collision playback is exercised by the scene test. This is temporary examiner/basic audio, not final voice/mix acceptance.

## Two-axis review

Standards: no documented-standard violations; two heuristic findings (historical replay audio and direct mutation of attempt command storage). Fixed with silent replay and `neutralize_driving`; reviewer recheck resolved both.

Spec: two findings (truck correction coherence and incomplete contextual highlights/pedal hints). Added a visual-only truck frame and control highlights/pedal hints, then inspected per-control captures. No collision-authoring visual offset was introduced. Reviewer recheck resolved both findings; human online feel remains the planned checkpoint.

## Ownership and limits

- 04: Extend the planar arcade hull to authoritative vertical support, bumps/ejection, recoverable falls, entrapment and rescue. Current simulation intentionally verifies a flat apron only.
- 06: Exactly three humans, rotating controls/host, must judge coordination, rear-steering necessity and Steam responsiveness; none of the agent harnesses substitutes for this checkpoint.
- 07: Replace rough truck/control geometry and floating physical labels with modeled instrument surfaces while preserving sight and access.
- 09–12: Replace the temporary offset gates/parking apron with the specified route. Add grade response/rollback and tyre-contact observations before claiming hill-start or full manoeuvre completion. Current controls and parking do not score manoeuvres.
- 23: Replace the temporary arrival request with complete offline examiner categories, repeat handling, reactions and mix coverage.

No unresolved ticket-03 criterion remains; broader full-vehicle and human acceptance remain with their owning tickets.
